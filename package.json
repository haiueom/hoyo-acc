{
	"name": "hoyo-acc",
	"type": "module",
	"scripts": {
		"dev": "pnpm run predb-local && wrangler types && wrangler dev",
		"deploy": "pnpm run predb-remote && wrangler types && wrangler deploy --minify",
		"cf-typegen": "wrangler types",
		"predb-remote": "wrangler d1 migrations apply DB --remote",
		"predb-local": "wrangler d1 migrations apply DB --local"
	},
	"dependencies": {
		"hono": "^4.11.7"
	},
	"devDependencies": {
		"wrangler": "^4.60.0"
	}
}
